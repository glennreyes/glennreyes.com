query getTalks($first: Int) {
  talks(first: $first) {
    date
    event
    eventUrl
    isLightningTalk
    location {
      city
      country
    }
    slidesUrl
    title
    venue
    videoUrl
  }
}
