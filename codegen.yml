schema: http://localhost:3000/.netlify/functions/graphql
documents: 'src/graphql/*.graphql'
overwrite: true
config:
  noNamespaces: true
generates:
  ./src/typings/__generated__/graphql.d.ts:
    plugins:
      - typescript-common
      - typescript-server
      - typescript-client
