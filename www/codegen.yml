schema: http://localhost:4000
documents: 'graphql/*.graphql'
overwrite: true
config:
  noNamespaces: true
generates:
  ./typings/__generated__/graphql.d.ts:
    plugins:
      - typescript-common
      - typescript-server
      - typescript-client
